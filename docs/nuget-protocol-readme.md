# Rewind.Protocol

Wire protocol implementation used by `Rewind.Sdk`.

Application developers normally install only `Rewind.Sdk`; NuGet resolves this
package automatically. Reference `Rewind.Protocol` directly only when building a
compatible Rewind transport or protocol integration.

The package targets .NET Framework 4.6.2 and .NET Standard 2.0.
